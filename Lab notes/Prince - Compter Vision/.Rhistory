ticktype = 'detailed')
prior_params <- c(0.5, 3, 1, 50)
prior_surf   <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, prior_params[1],
prior_params[2], prior_params[3], prior_params[4]))
}
)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed')
prior_params <- c(0.1, 3, 1, 50)
prior_surf   <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, prior_params[1],
prior_params[2], prior_params[3], prior_params[4]))
}
)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed')
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'yellow', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'blue',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
yellow
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
ribbon3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE)
ribbon3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.75)
ribbon3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
ribbon3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.2)
5
prior_params <- c(0.1, 3, 1, 50)
prior_surf   <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, prior_params[1],
prior_params[2], prior_params[3], prior_params[4]))
}
)
ribbon3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-10*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
2
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'yellow',
border = 'gray', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'none',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-5*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-2*max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(-max(prior_surf), max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 1)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 10)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 0.1)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 0.5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5, box = FALSE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 10)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 10,
box = FALSE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5,
box = FALSE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'NA',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, d = 5,
box = FALSE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 10)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 0.1)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 0.01)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = list(side = c('zmax', 'z')), col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.5,
d = 5)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.8,
d = 5)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.8,
d = 10)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.5,
d = 10)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.5,
d = 15)
0
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.5,
d = 10)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 1)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5,
shade = TRUE)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = TRUE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 0.5,
d = 5)
persp3D(mean_grid, sd_grid, likelihood_surf,
zlim = c(-5*max(likelihood_surf), max(likelihood_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Likelihood',
xlab = 'Mean', ylab = 'SD', zlab = 'Density', expand = 1,
d = 5)
prior_params <- c(0.1, 3, 4, 50)
prior_surf   <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, prior_params[1],
prior_params[2], prior_params[3], prior_params[4]))
}
)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
prior_params <- c(0.1, 5, 2, 50)
prior_surf   <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, prior_params[1],
prior_params[2], prior_params[3], prior_params[4]))
}
)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
prior_params <- c(0.1, 10, 5, 50)
prior_surf   <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, prior_params[1],
prior_params[2], prior_params[3], prior_params[4]))
}
)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = TRUE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
persp3D(mean_grid, sd_grid, prior_surf,
zlim = c(0, max(prior_surf)),
contour = FALSE, col = 'white',
border = 'blue', main = 'Prior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
post_params    <- prior_params
post_params[1] <- prior_params[1] + n_observations/2
post_params[3] <- prior_params[3] + n_observations
post_params[4] <- (prior_params[3]*prior_params[4] + sum(observations))/(prior_params[3] + n_observations)
post_params[2] <- (sum(apply(observations, function(t) t^2))/2
+ prior_params[2] + prior_params[3]*prior_params[4]^2/2
- ((prior_params[3]*prior_params[4] + sum(observations))^2 / 2*(prior_params[3] + n_observations)))
post_params[2] <- (sum(sapply(observations, function(t) t^2))/2
+ prior_params[2] + prior_params[3]*prior_params[4]^2/2
- ((prior_params[3]*prior_params[4] + sum(observations))^2 / 2*(prior_params[3] + n_observations)))
post_surf      <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, post_params[1],
post_params[2], post_params[3], post_params[4]))
}
)
persp3D(mean_grid, sd_grid, post_surf,
zlim = c(0, max(post_surf)),
contour = FALSE, col = 'white',
border = 'blue', main = 'Posterior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
post_params
post_params[2] <- ((sum(sapply(observations, function(t) t^2))/2)
+ prior_params[2] + ((prior_params[3]*prior_params[4]^2)/2)
- ((prior_params[3]*prior_params[4] + sum(observations))^2 / 2*(prior_params[3] + n_observations)))
post_params    <- prior_params
post_params[1] <- prior_params[1] + n_observations/2
post_params[3] <- prior_params[3] + n_observations
post_params[4] <- (prior_params[3]*prior_params[4] + sum(observations))/(prior_params[3] + n_observations)
post_params[2] <- ((sum(sapply(observations, function(t) t^2))/2)
+ prior_params[2] + ((prior_params[3]*prior_params[4]^2)/2)
- ((prior_params[3]*prior_params[4] + sum(observations))^2 / 2*(prior_params[3] + n_observations)))
post_surf      <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, post_params[1],
post_params[2], post_params[3], post_params[4]))
}
)
persp3D(mean_grid, sd_grid, post_surf,
zlim = c(0, max(post_surf)),
contour = FALSE, col = 'white',
border = 'blue', main = 'Posterior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
post_surf
post_params
post_params    <- prior_params
post_params[1] <- prior_params[1] + n_observations/2
post_params[3] <- prior_params[3] + n_observations
post_params[4] <- (prior_params[3]*prior_params[4] + sum(observations))/(prior_params[3] + n_observations)
post_params[2] <- ((sum(sapply(observations, function(t) t^2))/2)
+ prior_params[2] + ((prior_params[3]*prior_params[4]^2)/2)
- ((prior_params[3]*prior_params[4] + sum(observations))^2 / (2*(prior_params[3] + n_observations))))
post_params
post_surf      <- sapply(sd_grid,
function(sd) { # Each column corresponds to a mean value
sapply(mean_grid,
function(mu) NormInvGam(mu, sd, post_params[1],
post_params[2], post_params[3], post_params[4]))
}
)
persp3D(mean_grid, sd_grid, post_surf,
zlim = c(0, max(post_surf)),
contour = FALSE, col = 'white',
border = 'blue', main = 'Posterior',
xlab = 'Mean', ylab = 'SD', zlab = 'Density',
ticktype = 'detailed', shade = FALSE, expand = 0.5, d = 5)
S1 <- c(0.1, -0.5, 0.2, 0.7)
S2 <- c(1.1, 2, 1.4, 2.3)
S1_S2 <- c(S1, S2)
sum(S1_S2)
7.3/9
sum(sapply(S1_S2, function(t) t^2))
13.25/2
6.625 + 1 + (7.3^2/2.9)
alpha = 1
alpha_hat = 5
beta = 1
beta_hat = 26
clc
gamma(1)
sqrt(9)
26^5
7.3^2
53.29/2.9
13.25 + 1 - ((7.3^2)/(2*(1 + 8)))
11.29^5
